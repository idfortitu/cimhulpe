Public Class ancien_pan_beneficiaires

    ' ancien code qui récupère les données

    'If VerifConcessionnaire(LeConcessionnaire) Then       ' validation à voir
    '    Dim nbre_de_benef = PanInfosBenefs.FaddDDGCons.Rows.Count - 1
    '    If nbre_de_benef > 0 Then
    '        MessageBox.Show("if")
    '        Dim TabBenefs(nbre_de_benef) As Beneficiaire
    '        For i As Integer = 0 To nbre_de_benef - 1 Step +1
    '            TabBenefs(i) = New Beneficiaire()
    '            TabBenefs(i).Nom = PanInfosBenefs.FaddDDGCons.Rows(i).Cells(0).Value
    '            TabBenefs(i).Prenom = PanInfosBenefs.FaddDDGCons.Rows(i).Cells(1).Value
    '            TabBenefs(i).DateNaiss = PanInfosBenefs.FaddDDGCons.Rows(i).Cells(2).Value
    '            TabBenefs(i).LienParente = PanInfosBenefs.FaddDDGCons.Rows(i).Cells(3).Value
    '        Next
    '    End If
    '    MessageBox.Show("alo")
    'End If




End Class
